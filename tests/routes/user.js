'use strict';

const expect = require('chai').expect;

describe('Users API', function() {
  describe('GET', function() {
    it('should show simple example', function() {
      expect(1).to.eql(1);
    });
  });
});
